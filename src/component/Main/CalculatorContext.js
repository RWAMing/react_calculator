import { createContext } from 'react';

/** prevInput, setPrevInput, newInput, setNewInput, way, setWay, lastState, setLastState */
const CalculatorContext = createContext();
export default CalculatorContext;
